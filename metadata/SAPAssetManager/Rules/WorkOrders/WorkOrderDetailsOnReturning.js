import refreshToolbar from '../MobileStatus/RefreshDetailsToolbar';

export default function WorkOrderDetailsOnReturning(context) {
   
   //Set toolbar caption and enabled state
   return refreshToolbar(context);
   
}
